export class UsuarioModel {
    //estas propiedades me ayudaran para que los datos que meta en los campos, esos valores se pasen a estas propiedades (las de abajo) y asi sea mas facil mandarlas al backend
    email: string;
    password: string;
    nombre: string;
}
